#include "keys.h"

Keys::Keys()
{
   // gpk = new QByteArray();
   // usk = new QByteArray();
   // RL = new QByteArray();
}

/*
QDataStream &operator<<(QDataStream &out, const Keys &keys)
{
    out << keys.gpk << keys.usk << keys.RL;
    return out;
}

QDataStream &operator>>(QDataStream &in, Keys &keys)
{
    QByteArray gpk;
    QByteArray usk;
    QByteArray RL;


    in >> gpk >> usk >> RL;
    keys.gpk = gpk;
    keys.usk = usk;
    keys.RL = RL;

    return in;
}
*/
